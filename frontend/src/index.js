import App from './App'
import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <DictionaryPage />
  </React.StrictMode>,
  document.getElementById('main')
);
